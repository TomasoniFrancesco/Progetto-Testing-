\section{Implementazione Java}

\subsection{Struttura del progetto Maven}

Il progetto Java sta nella cartella \texttt{java/} ed è un progetto Maven normale, su
Java 17. Le dipendenze principali sono Spring Boot con Vaadin 24 per la UI, JUnit 5 e
Mockito per i test, JaCoCo per la copertura.

Il cuore sta nel package \texttt{it.tvsw.smartparking.core}, che contiene:

\begin{itemize}
    \item \texttt{StatoSistema}, \texttt{TipoUtente}, \texttt{TipoPosto}: tre enum, uguali ai domini del modello ASM;
    \item \texttt{Parcheggio}: il nucleo, ed è l'unica classe con i contratti JML (Sezione~\ref{sec:jml-ref});
    \item \texttt{SensorInput}: mette insieme i sei input monitorati di uno step;
    \item \texttt{Display}: un'interfaccia per le notifiche, che poi serve per Mockito;
    \item \texttt{SmartParkingFSM}: la macchina a stati vera e propria.
\end{itemize}

Fuori dal core ci sono solo la vista Vaadin e la classe di avvio di Spring Boot.

\texttt{Parcheggio} e \texttt{SmartParkingFSM} sono dichiarate \texttt{final} e tutte
le classi del core sono serializzabili: non era così all'inizio, sono due modifiche
arrivate dopo l'analisi statica e le spiego in Sezione~\ref{sec:spotbugs}.

\subsection{La macchina a stati}

\texttt{SmartParkingFSM} ha un metodo \texttt{step(SensorInput)} che fa la stessa cosa
della main rule dell'ASM: guarda in che stato siamo e chiama il gestore giusto.

\begin{lstlisting}[language=Java, caption={step() -- dispatch sullo stato corrente}]
public void step(SensorInput input) {
    if (input == null) {
        throw new IllegalArgumentException("input non puo' essere null");
    }
    switch (stato) {
        case IDLE:     gestisciIdle(input); break;
        case CHK_IN:   gestisciChkIn(); break;
        case VER:      gestisciVer(input); break;
        case NEG:      gestisciNeg(input); break;
        case INGR:     gestisciIngr(input); break;
        case CHK_OUT:  gestisciChkOut(input); break;
        case TARIF:    gestisciTarif(input); break;
        case USC:      gestisciUsc(input); break;
        default:
            throw new IllegalStateException("Stato non gestito: " + stato);
    }
}
\end{lstlisting}

Ogni \texttt{case} corrisponde a una regola dell'ASM, quindi il confronto tra i due
livelli si fa a colpo d'occhio. Il ramo \texttt{default} non è raggiungibile, dato che i
casi coprono tutti e otto i valori dell'enum: è una rete di sicurezza per il giorno in cui
qualcuno aggiungesse uno stato nuovo, e sarà l'unico branch che JaCoCo non riuscirà a
coprire (Sezione~\ref{sec:testing}). C'è però una differenza che è meglio dire subito. Nel
modello ASM il contatore viene scalato solo al transito vero e proprio, cioè in INGR,
mentre qui la classe \texttt{Parcheggio} decide e scala tutto insieme dentro
\texttt{assegnaPosto}, che viene chiamato da \texttt{gestisciVer}. Negli scenari di test
del progetto la differenza non si vede, perché nessuno va a controllare lo stato in mezzo
ai due passi, ed è una scelta fatta apposta per tenere la classe più semplice. Ne riparlo meglio
nell'ispezione del codice, in Sezione~\ref{sec:analisi-statica}.

\subsection{Interfaccia utente (Vaadin)}

La UI è una vista sola, \texttt{MainView}, che mostra lo stato corrente e quanti posti
sono ancora liberi. I sensori sono simulati con dei bottoni: ``Arriva auto
STD/DISABILE/ABBONATO'', ``Transito'', ``Pagamento'', ``Auto va via'' e ``Auto in
uscita''. Cliccandoli si fa avanzare la macchina a stati come se arrivassero gli input
dai sensori veri.

Questa è la pagina appena aperta: stato IDLE e i due contatori a 1.

\screenshot{vaadin_ui_idle}{UI Vaadin -- stato iniziale}{%
Pagina appena caricata: stato IDLE, un posto standard e uno disabile liberi.}

Qui è stato cliccato ``Arriva auto STD'' e poi ``Transito''. Siamo tornati in IDLE ma i posti
standard sono scesi a 0.


\screenshot{vaadin_ui_ingresso_std}{UI Vaadin -- dopo un ingresso STD}{%
Dopo ``Arriva auto STD'' e ``Transito'': stato IDLE e posti standard liberi a 0.}


Con il parcheggio pieno, se arriva un'altra auto compare il messaggio di accesso
negato.

\screenshot{vaadin_ui_neg}{UI Vaadin -- accesso negato a parcheggio pieno}{%
Con entrambi i contatori a 0, un nuovo arrivo mostra il messaggio ``Accesso negato''.}

E questo è lo stato TARIF: l'utente STD sta uscendo e il sistema si è fermato ad
aspettare il pagamento.

\screenshot{vaadin_ui_tarif}{UI Vaadin -- attesa pagamento}{%
Stato TARIF dopo aver cliccato ``Auto in uscita'' con un utente STD dentro.}



\subsection{Test end-to-end con Selenium}

La UI è provata anche con un test end-to-end in Selenium (\texttt{UISeleniumTest}). 

Quello che fa è: apre la pagina con ChromeDriver in modalità headless, aspetta che Vaadin
finisca di disegnare la pagina lato client (con \texttt{WebDriverWait}, altrimenti trova
il DOM vuoto), clicca ``Arriva auto STD'' e controlla che lo stato mostrato diventi INGR
oppure NEG. L'ho fatto partire da Eclipse come un normale test JUnit.

\screenshot{selenium_test_green}{Test Selenium end-to-end superato}{%
UISeleniumTest verde, eseguito con l'applicazione attiva su localhost:8080.}

\newpage