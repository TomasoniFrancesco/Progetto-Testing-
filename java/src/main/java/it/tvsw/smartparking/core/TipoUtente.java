package it.tvsw.smartparking.core;

/**
 * Categoria dell'utente rilevata dal totem, identica al dominio {@code TipoUtente}
 * definito in {@code src/SmartParking.asm}.
 */
public enum TipoUtente {
    STD,
    DISABILE,
    ABBONATO
}
