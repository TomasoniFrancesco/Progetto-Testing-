package it.tvsw.smartparking.core;

/**
 * Tipo di posto assegnato/occupato, identico al dominio {@code TipoPosto}
 * definito in {@code src/SmartParking.asm}.
 */
public enum TipoPosto {
    POSTO_STD,
    POSTO_DIS,
    NESSUNO
}
